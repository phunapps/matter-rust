//! Matter certificate type — placeholder until Task 6.
//!
//! `MatterCertificate` parser / serialiser is filled in during M2.1
//! phase 2 (Task 6). This stub keeps the workspace compiling while
//! Tasks 4 and 5 are in place.

/// A parsed Matter TLV certificate (§6.5).
///
/// Full parse/serialise implementation is added in Task 6.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct MatterCertificate;
