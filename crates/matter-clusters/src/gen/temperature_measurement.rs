//! TemperatureMeasurement cluster (0x0402).
//! @generated by `cargo xtask codegen` — do not edit.

#![allow(
    clippy::all,
    clippy::pedantic,
    dead_code,
    unreachable_pub,
    unused_imports
)]

use crate::datatypes::SemanticTagStruct;
use crate::error::ClusterError;
use crate::types::Nullable;
use matter_codec::{ContainerKind, Element, Tag, TlvReader, TlvWriter, Value};

/// Cluster ID.
pub const CLUSTER_ID: u32 = 0x0402;
/// Cluster revision.
pub const CLUSTER_REVISION: u16 = 5;

/// Command IDs (requests and responses).
pub mod command_id {}

/// Attribute IDs (cluster-specific).
pub mod attribute_id {
    /// `MeasuredValue`.
    pub const MEASURED_VALUE: u32 = 0x0000;
    /// `MinMeasuredValue`.
    pub const MIN_MEASURED_VALUE: u32 = 0x0001;
    /// `MaxMeasuredValue`.
    pub const MAX_MEASURED_VALUE: u32 = 0x0002;
    /// `Tolerance`.
    pub const TOLERANCE: u32 = 0x0003;
}

/// Decode the `MeasuredValue` attribute value.
///
/// # Errors
/// Returns [`ClusterError`] on a type mismatch or out-of-range value.
pub fn decode_measured_value(tlv: &[u8]) -> Result<Nullable<i16>, ClusterError> {
    let mut r = TlvReader::new(tlv);
    match r.next()? {
        Some(Element::Scalar {
            value: Value::Null, ..
        }) => Ok(Nullable::Null),
        Some(Element::Scalar {
            value: Value::Int(v),
            ..
        }) => {
            Ok(Nullable::Value(i16::try_from(v).map_err(|_| {
                ClusterError::InvalidLength("MeasuredValue")
            })?))
        }
        _ => Err(ClusterError::UnexpectedType {
            context: "MeasuredValue",
        }),
    }
}

/// Decode the `MinMeasuredValue` attribute value.
///
/// # Errors
/// Returns [`ClusterError`] on a type mismatch or out-of-range value.
pub fn decode_min_measured_value(tlv: &[u8]) -> Result<Nullable<i16>, ClusterError> {
    let mut r = TlvReader::new(tlv);
    match r.next()? {
        Some(Element::Scalar {
            value: Value::Null, ..
        }) => Ok(Nullable::Null),
        Some(Element::Scalar {
            value: Value::Int(v),
            ..
        }) => {
            Ok(Nullable::Value(i16::try_from(v).map_err(|_| {
                ClusterError::InvalidLength("MinMeasuredValue")
            })?))
        }
        _ => Err(ClusterError::UnexpectedType {
            context: "MinMeasuredValue",
        }),
    }
}

/// Decode the `MaxMeasuredValue` attribute value.
///
/// # Errors
/// Returns [`ClusterError`] on a type mismatch or out-of-range value.
pub fn decode_max_measured_value(tlv: &[u8]) -> Result<Nullable<i16>, ClusterError> {
    let mut r = TlvReader::new(tlv);
    match r.next()? {
        Some(Element::Scalar {
            value: Value::Null, ..
        }) => Ok(Nullable::Null),
        Some(Element::Scalar {
            value: Value::Int(v),
            ..
        }) => {
            Ok(Nullable::Value(i16::try_from(v).map_err(|_| {
                ClusterError::InvalidLength("MaxMeasuredValue")
            })?))
        }
        _ => Err(ClusterError::UnexpectedType {
            context: "MaxMeasuredValue",
        }),
    }
}

/// Decode the `Tolerance` attribute value.
///
/// # Errors
/// Returns [`ClusterError`] on a type mismatch or out-of-range value.
pub fn decode_tolerance(tlv: &[u8]) -> Result<u16, ClusterError> {
    let mut r = TlvReader::new(tlv);
    match r.next()? {
        Some(Element::Scalar {
            value: Value::Uint(v),
            ..
        }) => Ok(u16::try_from(v).map_err(|_| ClusterError::InvalidLength("Tolerance"))?),
        _ => Err(ClusterError::UnexpectedType {
            context: "Tolerance",
        }),
    }
}
